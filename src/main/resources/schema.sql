CREATE TABLE country (
                        name VARCHAR(200) PRIMARY KEY NOT NULL,
                        capital VARCHAR(200),
                        country_code VARCHAR(24),
                        population INT,
                        flag_file_url VARCHAR(255)
                     );